services:
  quran-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quran-api
    ports:
      - "8301:8301"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
